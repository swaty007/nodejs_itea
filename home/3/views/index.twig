{% extends 'layout.twig' %}

{% block body %}
  <h1>{{title}}</h1>
  <p>Welcome to {{title}}</p>
  {%  if user %}
    <p>Name: {{ user.name }}</p>
    <p>Age: {{ user.age }}</p>
  {%  endif %}

{% endblock %}
